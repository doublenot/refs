'use strict';

// const should = require('should');
// const Promise = require('bluebird');
// const path = require('path');
const td = require('testdouble');

describe('JSON Tests', () => {
  // const jsonProcessor = require('../lib/processor-json');

  beforeEach(() => {

  });

  afterEach(() => {
    td.reset();
  });

  it('should process the json file correctly', () => {

  });
});
