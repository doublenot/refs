'use strict';

// const should = require('should');
// const Promise = require('bluebird');
// const path = require('path');
const td = require('testdouble');

describe('INI Tests', () => {
  // const iniProcessor = require('../lib/processor-ini');

  beforeEach(() => {

  });

  afterEach(() => {
    td.reset();
  });

  it('should process the ini file correctly', () => {

  });
});
